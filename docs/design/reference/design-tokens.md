# Design tokens (raw)

Raw computed-style values captured from OpenRefine (e.g. via a browser DevTools/extension), pasted as-is. Don't synthesize or interpret here — that happens in `../ui-design-spec.md` during the business-analyst pass.

_Not yet populated._

## Colors

## Typography

## Spacing

## Other (shadows, border-radius, etc.)
